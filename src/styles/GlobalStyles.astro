---

---

<style is:global lang="scss">
  @use "./core/base";
  @use "./core/typography";
  @use "./core/components";
  @use "./core/layout";

  // Desactiva la prefencia del scroll del usuario
  @media (prefers-reduced-motion: reduce) {
    :global(html) {
      scroll-behavior: auto;
    }
  }
</style>
